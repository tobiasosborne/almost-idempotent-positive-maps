# PROOF KIT — lem-classical-equiv (groundability verdict: A, build may proceed)

> Generated by the groundability-gate workflow (wf_9df27589-bf0). BUILD artifact (scratch), not canonical.
> Ground truth remains refs/. Verdict A = build proceeds; gaps are derived as nodes, no source to acquire.


## Contract (af root — byte-verbatim from argument/lemmas/lem-classical-equiv.md line 4)

```
The signed-idempotent and stochastic-idempotent formulations of classical stability are equivalent up to universal constants: Q row-stochastic with ||Q^2-Q|| <= eta gives P=theta(2Q-1) signed affine retraction with ||P-Q|| <= C eta and neg mass delta <= C eta, and conversely row-normalising p_i^+ gives Q with ||P-Q|| <= 2 delta, ||Q^2-Q|| <= 6 delta+4 delta^2.
```


## Verdict rationale

Verdict A (acceptable — build can proceed; all gaps are derivable as nodes from present facts, no un-acquired source needed). The load-bearing functional-calculus / "spectral separation" bound is NOT a hand-wave with no byte-source: I re-read refs/kitaev-2405.02434/approximate_algebras.tex:503-533 verbatim and confirm the bytes DO state, at full Banach-algebra generality, (i) the convergence f(X)=a0 I+sum a_n(X-x0 I)^n for ||X-x0 I||<r, (ii) the exact inequality ||f(X)-f(x0 I)|| <= sum_{n>=1}|a_n| ||X-x0 I||^n (this IS the Step-2 ||R-I|| bound the note hand-waved), (iii) f(x)=x^alpha well-defined for ||X-x0 I||<x0, and (iv) Prop_P: ||P^2-P||<=delta<1/4 ==> theta(2P-I) is EXACTLY idempotent, commutes, ||~P-P||<=||2P-I|| O(delta), via X=2P-I, ||X^2-I||<=4delta<1. So P^2=P exact and the O(eta) closeness ARE grounded. THREE residual gaps are all derivable, none requiring acquisition: (a) the EXPLICIT universal constant C (Kitaev states only big-O; the contract demands explicit C) — derived inside the workspace from the verbatim sum-bound (iii) + the binomial coefficients C(-1/2,n); (b) calculus multiplicativity f(X)g(X)=(fg)(X) used for sgn(S)^2=I and R^2=(S^2)^{-1} — NOT in the bytes (line 535 only mentions holomorphic calculus to dismiss it), derived as a Cauchy-product node; (c) the M_n(R) max-row-ell^1 operator norm = unital Banach algebra + submult/triangle — NOT byte-stated anywhere (exhaustive grep of all 7 present payloads found zero hits for the max-row formula), derived as a one-line node from the generic operator-norm sup-definition (the established repo pattern at lem-P-properties node 1.1.2). The forward direction is moreover the M_n(R) specialization of the af-VALIDATED lem-P-properties, which already discharges P^2=P / P1=1 / ||P-Phi||<=C eta against the same Kitaev Prop_P; importing it as a black-box DAG dependency removes most re-grounding. The converse direction is fully elementary (an exact ring identity Q^2-Q=(Q-P)Q+P(Q-P)-(Q-P) under P^2=P, plus disjoint-support ell^1 arithmetic) — every step derivable, the only external-style inputs being the two definitions. NOT verdict R: no fact is both un-quotable AND un-derivable from present facts; nothing must be acquired. NOT verdict G: the contract's explicit-constant conclusion, multiplicativity, and the matrix-norm arena are genuinely not byte-verbatim, so they must be discharged as proof nodes, not passed off as cited.


## Structure: 11 nodes, depth 3, needs_factoring=False

Forward = N1..N7 (Mn(R) Banach algebra + Kitaev Taylor/Prop_P, instantiated DIRECTLY at Mn(R) — do NOT import lem-P-properties, arena mismatch B(H)_sa vs Mn(R)). Converse = N8..N10 (elementary ring identity + ell^1). ROOT assembles.


## NODE PLAN


### N1  — depends_on: (none)
ARENA: M_n(R) with ||A||=max_i sum_j|A_ij| (induced ell^inf->ell^inf) is a unital Banach algebra (||I||=1, triangle, exact submultiplicativity ||AB||<=||A|| ||B||, max-row-ell^1 closed form, completeness). Shared by both directions.

### N2  — depends_on: N1
FORWARD spectral defect: Q row-stochastic (def-stochastic) => ||Q||=1, S:=2Q-I has S1=1, ||S||<=3, ||S^2-I||=4||Q^2-Q||=4eta<=4eta_0<1 for eta_0<1/4 (GT-small-eta-threshold).

### N3  — depends_on: N2
FORWARD inverse-sqrt + explicit C: R=(S^2)^{-1/2}=sum C(-1/2,n)(S^2-I)^n converges (GT-funcalc-taylor-bound), R1=1, and ||R-I||<=C_1(eta_0)||S^2-I||<=4 C_1(eta_0) eta with explicit C_1(eta_0)=sum_{n>=1}|C(-1/2,n)|(4eta_0)^{n-1}<infinity (|C(-1/2,n)|=C(2n,n)/4^n).

### N4  — depends_on: N3
FORWARD calculus multiplicativity: R^2=(S^2)^{-1} (Cauchy product, GT-funcalc-taylor-bound + N1 submult), hence sgn(S):=SR satisfies sgn(S)^2=S^2 R^2=I exactly.

### N5  — depends_on: N4
FORWARD P is an exact signed affine retraction: P:=theta(2Q-I)=(1/2)(I+sgn(S)) has P^2=P exactly (N4 + GT-kitaev-spectral-propP) and P1=(1/2)(1+sgn(S)1)=(1/2)(1+S R 1)=1 (S1=1, R1=1).

### N6  — depends_on: N3, N5
FORWARD closeness: P-Q=S(R-I)/2 => ||P-Q||<=(||S||/2)||R-I||<=(3/2)(4 C_1 eta)=C eta with explicit universal C:=6 C_1(eta_0) (N1 submult, N2 ||S||<=3, N3 ||R-I|| bound).

### N7  — depends_on: N6
FORWARD negative mass: Q>=0 entrywise (def-stochastic) => neg(p_i)<=||p_i-q_i||_1 entrywise, so delta=max_i neg(p_i)<=||P-Q||<=C eta (same universal C). Closes FORWARD: P signed affine retraction, ||P-Q||<=C eta, delta<=C eta.

### N8  — depends_on: N1
CONVERSE construction + distances: P signed affine retraction (P1=1, P^2=P, def-stochastic); disjoint-support split p_i=p_i^+-p_i^-, ||p_i^+||_1=1+a_i, q_i=p_i^+/(1+a_i) => Q row-stochastic (||Q||=1), ||p_i-q_i||_1=2a_i => ||P-Q||<=2 delta, and ||p_i||_1=1+2a_i => ||P||<=1+2 delta.

### N9  — depends_on: N1, N8
CONVERSE algebraic identity: with D=Q-P and P^2=P exactly, Q^2-Q=(Q-P)Q+P(Q-P)-(Q-P)=PD+DP+D^2-D (exact ring identity in M_n(R)).

### N10  — depends_on: N9
CONVERSE defect bound: triangle+submult (N1) on N9 with ||Q-P||<=2 delta, ||Q||=1, ||P||<=1+2 delta (N8) => ||Q^2-Q||<=2 delta+(1+2 delta)(2 delta)+2 delta=6 delta+4 delta^2. Closes CONVERSE: ||P-Q||<=2 delta, ||Q^2-Q||<=6 delta+4 delta^2.

### ROOT  — depends_on: N7, N10
lem-classical-equiv contract: the two formulations are equivalent up to universal constants (FORWARD N7 + CONVERSE N10), VERBATIM root = registry contract.


## EXTERNALS (byte-grounded in PRESENT refs — copy bytes VERBATIM; reuse proofs/lem-P-properties/externals where noted)


### GT-funcalc-taylor-bound
- source: kitaev-2405.02434 @ refs/kitaev-2405.02434/approximate_algebras.tex:503-516
- statement: Kitaev Taylor functional calculus: for a Banach algebra B and f(x)=sum a_n(x-x0)^n with convergence radius >= r, and X in B with ||X-x0 I||<r, f(X)=a0 I+sum_{n>=1}a_n(X-x0 I)^n is well-defined, commutes with X, and ||f(X)-f(x0 I)|| <= sum_{n>=1}|a_n| ||X-x0 I||^n; f(x)=x^alpha is well-defined for ||X-x0 I||<x0. (Instantiated at B=M_n(R), f(x)=x^{-1/2}, x0=1, r=1, X=S^2 to define R=(S^2)^{-1/2} and bound ||R-I||.) CAVEAT: this supplies the qualitative form and the sum-bound; the EXPLICIT universal C is derived in node-binomial-coeffs-and-C, not read off these bytes.
- VERBATIM bytes (re-extract/confirm from the file; do not retype from memory):
```
Let $\calB$ be a Banach algebra and $f$ a function of one complex variable represented by its Taylor series $f(x)=\sum_{n=0}^{\infty}a_n(x-x_0)^n$ with the convergence radius $\ge r$. Then $f(X)$ is well-defined, commutes with $X$, and satisfies a certain bound for all elements $X\in\calB$ sufficiently close to $x_0I$:
\begin{gather}
\label{Taylor_simple}
f(X)=a_0I+\sum_{n=1}^{\infty}a_n(X-x_0I)^n\quad\:
\text{if }\, \|X-x_0I\|<r,\\
\label{Taylor_simple_bound}
\|f(X)-f(x_0I)\|\le \sum_{n=1}^{\infty}|a_n|\ts\|X-x_0I\|^n.
\end{gather}
For example, let $\alpha$ be an arbitrary complex number, $x_0$ a positive number, and let $f(x)=x^{\alpha}$. Then $f(X)$ is well-defined if $\|X-x_0I\|<x_0$.
```

### GT-kitaev-spectral-propP
- source: kitaev-2405.02434 @ refs/kitaev-2405.02434/approximate_algebras.tex:514-533
- statement: Kitaev sgn/Prop_P: sgn(X)=X(X^2)^{-1/2} (defined for ||X^2-x0^2 I||<x0^2), sgn(X)^2=I, ||sgn(X)-X|| <= ||X|| O(||X^2-I||) if ||X^2-I||<1; and Prop_P: P in a Banach algebra with ||P^2-P||<=delta<1/4, ~P=theta(2P-I)=(1/2)(I+sgn(2P-I)) satisfies ~P^2=~P EXACTLY, ~P commutes with P, ||~P-P|| <= ||2P-I|| O(delta), proved via X=2P-I which has ||X^2-I||<=4delta<1. CAVEAT: constants are literal big-O; the contract's explicit C must be derived (node-binomial-coeffs-and-C). Kitaev's home norm is the eps-Banach/cb-norm; the M_n(R) operator-norm arena is the project adaptation (node-mn-banach-algebra).
- VERBATIM bytes (re-extract/confirm from the file; do not retype from memory):
```
\begin{equation}\label{abs_sgn}
|X|=(X^2)^{1/2},\qquad \sgn(X)=X(X^2)^{-1/2}\qquad\quad
\bigl(\|X^2-x_0^2I\|<x_0^2\bigr).
\end{equation}
If $X$ is an approximate solution of the equation $X^2=I$, then a nearby exact solution, commuting with $X$, is given by $\sgn(X)$. Indeed, $\sgn(X)^2=I$ for all $X$, and
\begin{equation}
\|\sgn(X)-X\|\le \|X\|\,O(\|X^2-I\|)\quad\:
\text{if }\, \|X^2-I\|<1.
\end{equation}

\begin{Proposition}\label{prop_P}
Let $P$ be an element of a Banach algebra such that $\|P^2-P\|\le\delta<1/4$, and let
\begin{equation}
\wt{P}=\theta(2P-I),\qquad
\text{where}\quad \theta(X)=\frac{1}{2}\bigl(I+\sgn(X)\bigr).
\end{equation}
Then $\wt{P}^2=\wt{P}$,\, $\wt{P}$ commutes with $P$, and $\|\wt{P}-P\|\le \|2P-I\|\ts O(\delta)$.
\end{Proposition}
\begin{proof} The statement follow from the preceding discussion applied to $X=2P-I$, which satisfies the inequality $\|X^2-I\|\le 4\delta<1$.
\end{proof}
```

### GT-small-eta-threshold
- source: kitaev-2405.02434 @ refs/kitaev-2405.02434/approximate_algebras.tex:525,532,2176
- statement: The eta<1/4 convergence threshold is grounded byte-verbatim: Prop_P requires ||P^2-P||<=delta<1/4, and its proof states X=2P-I satisfies ||X^2-I||<=4delta<1; the applied tilde-Phi instance is described as a Taylor expansion in 4(Phi-Phi^2) which converges if eta<1/4. Under the rename P->Q, delta->eta, X=2P-I->S=2Q-I this is exactly ||S^2-I||=4||Q^2-Q||=4eta<1 for eta<1/4.
- VERBATIM bytes (re-extract/confirm from the file; do not retype from memory):
```
Let $P$ be an element of a Banach algebra such that $\|P^2-P\|\le\delta<1/4$, and let
[...]
\begin{proof} The statement follow from the preceding discussion applied to $X=2P-I$, which satisfies the inequality $\|X^2-I\|\le 4\delta<1$.
[...]
The right-hand side involves a Taylor expansion in $4(\Phi-\Phi^2)$, which converges if $\eta<1/4$.
```


## DERIVED NODES (proven inside the workspace, NOT cited)


### node-mn-banach-algebra
- M_n(R) under the operator norm ||A||=||A||_{inf->inf}=max_i sum_j |A_ij| (induced by ||x||_inf=max_k|x_k| on R^n) is a unital Banach algebra: ||I||=1, triangle inequality ||A+B||<=||A||+||B||, and submultiplicativity ||AB||<=||A|| ||B|| (exact, no 1+eps slack). Includes the closed-form max-row-ell^1 evaluation: for ||x||_inf<=1, |(Ax)_i|<=sum_j|A_ij|, with equality at x_j=sign(A_ij), so ||A||=max_i sum_j|A_ij|; finite-dim => complete.
- derived_from: Generic operator-norm sup-definition (Kitaev approximate_algebras.tex:639-640 abstract ||w||=sup_{||Z||<=1}||w(Z)||; Kitaev:642 'bounded linear operators form a Banach algebra') + standard finite-dimensional real analysis (the max-row formula and submult/triangle from the sup-definition, exactly as proved for End(B(H)_sa) in proofs/lem-P-properties node 1.1.2). The max-row formula itself is NOT byte-stated in any present ref -- flagged extraction-level, proved inline.

### node-binomial-coeffs-and-C
- For f(x)=x^{-1/2}, x0=1: a_n=C(-1/2,n) with |a_n|=C(2n,n)/4^n>=0 (the coefficients C(-1/2,n)(-1)^n are the nonnegative central-binomial coefficients), so sum_{n>=0}|a_n| t^n = (1-t)^{-1/2} for 0<=t<1; hence with t=||S^2-I||<=4eta_0<1, C_1(eta_0):=sum_{n>=1}|a_n|(4eta_0)^{n-1}<infinity and is EXPLICIT. This converts Kitaev's big-O closeness bound into the explicit ||R-I|| <= C_1(eta_0)||S^2-I|| <= 4 C_1(eta_0) eta, and ultimately the single explicit universal C=6 C_1(eta_0).
- derived_from: GT-funcalc-taylor-bound (the verbatim sum_{n>=1}|a_n| ||X-x0 I||^n bound + f(x)=x^alpha at x0=1) + an internal scalar real-analysis computation |C(-1/2,n)|=C(2n,n)/4^n (one-line, no external). This is the de-hand-waved 'spectral separation estimate' made explicit.

### node-calculus-multiplicativity
- For a fixed X with ||X-x0 I||<r the assignment g->g(X) on absolutely-convergent power series is multiplicative: f(X)g(X)=(fg)(X), proved via the Cauchy product of two absolutely convergent series in the single commuting element Y=X-x0 I (absolute convergence + submultiplicativity ||Y^{i+j}||<=||Y^i|| ||Y^j||). Specific instance needed: R^2=(S^2)^{-1} (f(x)=x^{-1/2} squared = x^{-1} on the disc), giving sgn(S)^2=S^2 R^2=I exactly; and R*1=1 (a0=f(1)=1, (S^2-I)1=0).
- derived_from: GT-funcalc-taylor-bound (convergence of f(X) as absolutely-convergent series in Y) + node-mn-banach-algebra (submultiplicativity for the Cauchy product). NOT in Kitaev's bytes (line 535 only dismisses the holomorphic calculus); discharged as an internal Cauchy-product node.

### node-forward-via-P-properties
- FORWARD assembly: instantiating the function calculus in M_n(R) at S=2Q-I (||S^2-I||=4eta<1 by GT-small-eta-threshold), R=(S^2)^{-1/2}, sgn(S)=SR, P=theta(2Q-I)=(1/2)(I+sgn(S)) gives P^2=P exactly and P1=1; and P-Q=S(R-I)/2 gives ||P-Q||<=(||S||/2)||R-I||<=(3/2)(4 C_1 eta)=6 C_1(eta_0) eta=:C eta with ||S||<=3. This is the M_n(R)-specialization of the af-VALIDATED lem-P-properties (P^2=P node 1.3, P1=1 node 1.4, ||P-Phi||<=C eta node 1.5), with End(B(H)_sa)->M_n(R).
- derived_from: GT-kitaev-spectral-propP (P^2=P exact, commutes, O(eta) closeness form) + node-mn-banach-algebra (arena, ||S||<=3, submult) + node-binomial-coeffs-and-C (explicit C) + node-calculus-multiplicativity (sgn(S)^2=I, R1=1) + GT-small-eta-threshold (eta<1/4) + stochastic def (Q>=0, Q1=1 => ||Q||=1, S1=1) [def-stochastic, hypothesis]. Optionally imports lem-P-properties as a black-box DAG dependency.

### node-forward-negmass
- FORWARD negative mass: since Q>=0 entrywise, for each (i,j) max(-P_ij,0)<=max(Q_ij-P_ij,0)<=|P_ij-Q_ij|; summing over j gives neg(p_i)<=||p_i-q_i||_1, so delta=max_i neg(p_i) <= ||P-Q||_{inf->inf} <= C eta. Commutative analogue of lem-P-properties node 1.7 (delta-positivity).
- derived_from: node-forward-via-P-properties (||P-Q||<=C eta) + def-stochastic (neg(p_i)=sum_j max(-P_ij,0), Q>=0 entrywise) + node-mn-banach-algebra (||P-Q||=max_i sum_j|P_ij-Q_ij|=max_i ||p_i-q_i||_1). Elementary entrywise inequality; no external.

### node-converse-pos-neg-split
- CONVERSE setup: every real row decomposes p_i=p_i^+ - p_i^- (disjoint support, p_i^+(j)=max(p_ij,0), p_i^-(j)=max(-p_ij,0)), so ||p_i||_1=||p_i^+||_1+||p_i^-||_1 and ||a p_i^+ - b p_i^-||_1=a||p_i^+||_1+b||p_i^-||_1 for a,b>=0. With total mass sum_j p_ij=1 (from P1=1): ||p_i^+||_1=1+a_i (a_i=||p_i^-||_1=neg(p_i)<=delta). Define q_i:=p_i^+/(1+a_i) (well-defined since 1+a_i>=1>0); q_i>=0, ||q_i||_1=1, so Q row-stochastic, ||Q||=1.
- derived_from: Elementary scalar sign case-split / ell^1 additivity of disjoint supports (no external) + def-stochastic (neg mass; signed measure of total mass 1; row-stochastic = Q>=0, Q1=1, hypothesis P1=1) + node-mn-banach-algebra (||Q||=max_i ||q_i||_1=1).

### node-converse-distances
- CONVERSE distances: p_i-q_i = p_i^+ (a_i/(1+a_i)) - p_i^- has disjoint-support parts, so ||p_i-q_i||_1=(1+a_i)(a_i/(1+a_i))+a_i=2a_i, giving ||P-Q||=max_i 2a_i<=2 delta; and ||p_i||_1=(1+a_i)+a_i=1+2a_i, giving ||P||=max_i(1+2a_i)=1+2 delta (P1=1 alone does NOT bound ||P||; the +/- split does).
- derived_from: node-converse-pos-neg-split (the split, normaliser 1+a_i, disjoint-support ell^1 additivity) + node-mn-banach-algebra (||.||=max-row-ell^1) + def-stochastic (a_i<=delta).

### node-converse-idempotency-identity
- CONVERSE algebraic core: with D=Q-P and P^2=P EXACTLY, the exact ring identity Q^2-Q = (Q-P)Q + P(Q-P) - (Q-P) = PD+DP+D^2-D holds (Q^2=(P+D)^2=P^2+PD+DP+D^2=P+PD+DP+D^2, subtract Q=P+D). Pure associative-unital-ring computation, no approximation.
- derived_from: Hypothesis P^2=P (def-stochastic / converse hypothesis) + distributivity & associativity of M_n(R) (node-mn-banach-algebra arena). Closable by ring normalization; no external.

### node-converse-defect-bound
- CONVERSE conclusion: taking ||.||_{inf->inf} on the identity, triangle + submultiplicativity give ||Q^2-Q|| <= ||Q-P|| ||Q|| + ||P|| ||Q-P|| + ||Q-P||; substituting ||Q-P||<=2 delta, ||Q||=1, ||P||<=1+2 delta yields ||Q^2-Q|| <= 2 delta + (1+2 delta)(2 delta) + 2 delta = 6 delta + 4 delta^2. Combined with ||P-Q||<=2 delta this closes the converse.
- derived_from: node-converse-idempotency-identity (the identity) + node-mn-banach-algebra (triangle, submult) + node-converse-distances (||Q-P||<=2 delta, ||P||<=1+2 delta) + node-converse-pos-neg-split (||Q||=1).


## HONEST FLAGS the prover MUST carry (and the verifier MUST scrutinize, not gloss)

1. Kitaev gives only big-O closeness constants; the contract's EXPLICIT universal C = 6*C_1(eta_0) is DERIVED (node-binomial-coeffs-and-C, |C(-1/2,n)|=C(2n,n)/4^n), never cited.
2. CALCULUS MULTIPLICATIVITY (sgn(S)^2=I via R^2=(S^2)^{-1}): fresh grounding probe says this is NOT byte-stated in Kitaev (line 535 only dismisses holomorphic calculus) and must be a Cauchy-product NODE (node-calculus-multiplicativity). BUT the existing proofs/lem-P-properties external GT-funcalc (0f14e166) bundles 'the calculus is multiplicative f(X)g(X)=(fg)(X)' as cited @ :503-532,:642. TENSION: build it as a derived Cauchy-product node (conservative) OR justify citing GT-funcalc; either way FLAG for the verifier to byte-check against refs/kitaev.
3. The Mn(R) max-row-ell^1 operator-norm CLOSED FORM is NOT byte-stated in any present ref (exhaustive grep = 0 hits). Derive node-mn-banach-algebra inline from the generic operator-norm sup-definition (Kitaev :639-642), flag EXTRACTION-LEVEL (established lem-P-properties node 1.1.2 pattern).
4. Hypotheses (eta<=eta_0<1/4; delta<=1 for the converse if needed) -> one GT-hyp external (skip_noquote).