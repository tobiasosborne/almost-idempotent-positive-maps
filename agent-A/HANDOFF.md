# HANDOFF — almost-idempotent positive maps / approximate Jordan algebras

**For:** a fresh agent picking up this project (as Agent A, or as a solo).
**Last updated:** 2026-06-04 by Agent A.
**Read this top-to-bottom once; then read `agent-a-findings` and `agent-b-findings` (the live coordination log) and `report/main.tex`.**

---

## 0. One-paragraph summary

We are generalising Kitaev's theorem on almost-idempotent quantum channels (arXiv:2405.02434) from **completely positive** maps to merely **positive** maps. Kitaev: an almost-idempotent unital CP map on `B(H)` has near-fixed-points forming an approximate **C\*-algebra**, and every finite-dim approximate C\*-algebra is close to a genuine one. Our thesis: for merely positive maps the invariant is a **Jordan (JB) algebra**, not an associative one — because positive maps obey Kadison's inequality `Φ(a)²≤Φ(a²)` but not the Schwarz inequality `Φ(a)*Φ(a)≤Φ(a*a)`. The setting paper is van Luijk–Wilming (arXiv:2604.08380), which shows the right invariant for positive trace-preserving maps is the (Jordan) `J*`-algebra. **North star:** a complete, rigorous theorem + proof of the Jordan analogue, agreed by consensus between two collaborating agents (A and B).

**Status in one line:** the channel→algebra "bridge" half is **PROVED** at error `O(√η)` (complete, dimension-free, CP-free); the abstract structure theorem (Kitaev's hard half) is **OPEN**, with the precise obstruction now isolated; exact positive-map factorisation is **OPEN** (a generic rounding step is provably false).

**Newest result (2026-06-04, smoke test):** the isolated Layer-1 dimension obstruction — is the splitting constant `K=‖s‖` of the Jordan coboundary `d¹` dimension-free? — was probed numerically out to far larger `N` than before. **The canary does NOT fire:** `‖s‖_F` bounded (exact) and `‖s_F‖_op` flat→decreasing for spin factors to `N=41` (trustworthy), plateauing for `H_n(ℝ/ℂ)` to `N=36`. Encouraging evidence for `prop:error-reduction`, NOT a proof (lower-bound estimates; Frobenius-minimal splitting; module `M=J`). See `agent-A/experiments/jordan-coboundary/CANARY_SMOKE.md`.

---

## 1. The goal, precisely

Two papers (TeX sources are local — see §6):
- **Kitaev**, *Almost-idempotent quantum channels and approximate C\*-algebras*, arXiv:2405.02434. Defines `η`-idempotent UCP `Φ` (`‖Φ²−Φ‖_cb≤η`), builds an `ε`-C\*-algebra `A=Im(θ(2Φ−1))` with Choi–Effros product `X⋆Y=Φ̃(XY)`, `ε=O(η)`; main theorem: every finite-dim `ε`-C\*-algebra is `O(ε)`-isomorphic to a genuine C\*-algebra, **dimension-free**; corollary: approximate factorisation of the channel by UCP maps.
- **van Luijk–Wilming**, *Sufficiency and Petz recovery for positive maps*, arXiv:2604.08380. For positive (not CP) maps the sufficient invariant is a **Jordan** `J*`-algebra (the minimal sufficient Jordan algebra), generated by Neyman–Pearson tests; positive maps satisfy the **Jordan–Schwarz** inequality `{Ta*,Ta}≤T{a*,a}`; the multiplicative domain of a UP map is a `J*`-algebra (vs a `*`-algebra for UCP).

**Target theorems (consensus two-layer formulation).**
- **Layer 2 — the bridge (PROVED, α=1/2).** `Φ:B(H)_sa→B(H)_sa` unital positive, `‖Φ²−Φ‖≤η` (operator norm of the map). Let `P=θ(2Φ−𝟏)` (spectral idempotent), `A=Im P`, product `a•b=P(a∘b)` with `a∘b=½(ab+ba)`. Then `A` is an **ε-JB algebra** with `ε=O(√η)`. Unconditional, dimension-free, no dilation/CP used.
- **Layer 1 — the structure theorem (OPEN).** Every finite-dimensional ε-JB algebra is `Cε`-Jordan-isomorphic to a genuine finite-dimensional JB-algebra, with **dimension-free** `C`. This is Kitaev's main theorem, Jordan-ised. NOT yet proved.
- **Exact factorisation (OPEN).** Composing the two would give: positive almost-idempotent `Φ` factorises through a genuine special JB-algebra by unital positive maps with `O(√η)` error. Obstruction: a generic `O(ε)` positivity-rounding step is **false** (spin-factor counterexample, B); needs either a concrete near-JC-subalgebra or "near-positive projection stability".

**The ε-JB algebra (the central definition; consensus).** A finite-dim real **order-unit space** `(A,𝟏,≤)` with order-unit norm, and a commutative bilinear product `∘` for which **commutativity `a∘b=b∘a` and the unit law `𝟏∘a=a` are EXACT**, satisfying:
- (JB1) `‖a∘b‖≤(1+ε)‖a‖‖b‖`;
- (JB2) `‖a∘a‖≥(1−ε)‖a‖²`;
- (JB3) `a∘a≥−ε‖a‖²·𝟏` (approximate positivity of squares);
- (JB4) `‖((a∘a)∘b)∘a−(a∘a)∘(b∘a)‖≤ε‖a‖³‖b‖` (approximate Jordan identity).
At `ε=0` these degenerate exactly to the JB-algebra axioms (Hanche-Olsen–Størmer §3.1.3–3.1.4). **Key design point:** order and norm are *exact* (inherited from `B(H)`); only the product is approximate — strictly cleaner than Kitaev's ε-C\* algebras where even the norm axiom is approximate.

---

## 2. What is PROVED, and the ideas

**The bridge theorem (Layer 2).** Full proof in `agent-B/theory/theorem-B-algebraic-bridge.md`, transcribed into `report/sections/06-bridge-theorem.tex`. Agent A verified it line-by-line (recorded in `agent-a-findings` §10). Key mechanism:
1. `P=θ(2Φ−𝟏)=½(𝟏+sgn(2Φ−𝟏))` via binomial series; valid for `η<1/4`; `P²=P`, `P(𝟏)=𝟏`, `†`-preserving, `‖P−Φ‖≤O(η)`, `‖P‖≤1+O(η)`, `δ`-positive.
2. **Square holes** `q_r=P(r²)−r²∈Ker P` are *almost-positive null*: `q_r≥−Cη‖r‖²𝟏` (Kadison) and `‖P(q_r²)‖≤Cη‖r‖⁴` (because `0≤y≤c𝟏 ⇒ y²≤cy` and `Φ` positive). This is the crux fact.
3. Work in the seminorm `‖x‖²_ω=ω(x²)`, `ω=ρ∘Φ` (a state). `P` is an almost-contraction there, so `Im P ⟂ Ker P` up to `O(√η)`.
4. **Two-hole vs one-hole:** terms with two holes are `O(η)` (Cauchy–Schwarz in `‖·‖_ω`: `(HH)`); terms with one hole are `O(√η)` (`(FI),(HZ)`). The Jordan-identity defect reduces, via the **exact** ambient Jordan identity of `B(H)_sa` (which kills the leading term `P((a²∘b)∘a)−P(a²∘(b∘a))=P(0)=0`), to `O(√η)`.

**Why Jordan, not associative (the conceptual core).** Kitaev's C\*-norm axiom uses Schwarz `Φ(X)*Φ(X)≤Φ(X*X)` (needs 2-positivity). Merely positive maps give only Kadison `Φ(a)²≤Φ(a²)` for `a=a*` — self-adjoint elements and squares, i.e. Jordan data. So the associative product must be replaced by `a∘b=½(ab+ba)`; the theory reorganises around it. (See memory `project-kadison-forces-jordan`.)

---

## 3. What is OPEN, with the precise obstructions

**(A) Layer-1 structure theorem.** Strategy mirrors Kitaev: (a) approximate Jordan frame (orthogonal idempotents); (b) approximate Peirce decomposition; (c) approximate coordinatization (Jordan–von Neumann–Wigner); (d) **error reduction** (the cohomological heart). Cited inputs: `H¹=H²=0` for finite-dim semisimple Jordan algebras (Whitehead lemmas: Jacobson, Penico; Chu–Russo); `Aut(J)` compact for Euclidean `J` (Faraut–Korányi); `M(J)` (multiplication algebra) semisimple.

  *The obstruction (now isolated numerically — see `agent-A/experiments/jordan-coboundary/REPORT.md`).* The error-reduction needs a **dimension-free bounded right-inverse `s` of the Jordan coboundary `d¹`** (`‖s‖=1/σ_min(d¹)`). Numerics (validated: `dim Ker d¹=dim Der(J)` matched exactly for `H_n(C)`, `H_n(R)`, spin factors):
  - **Frobenius/trace norm: BOUNDED** (`‖s‖≤1`, decreasing). 
  - **Operator/order-unit norm — the one the theorem needs:** REPORT.md left it **UNRESOLVED** at `n≤6` (lower bounds sub-`√n`; crude upper bound `~n`). **Update (2026-06-04 smoke test, `canary_smoke.py`/`CANARY_SMOKE.md`):** extended to spin `V_n` `N=41` (trustworthy: closed-form order-unit norm via extreme-point computation) and `H_n(ℝ/ℂ)` `N=36`. `‖s_F‖_op` is **flat→decreasing (spin) / plateauing (H_n)** — *no* dimension dependence detected. Mechanism: `σ_min(d¹)` never collapses (spin flat ~1.6), `σ_max~√N`, so `κ=σ_max/σ_min~√N` grows but `1/σ_min` (which feeds `K`) stays bounded. Caveats: lower-bound estimates (not a proof); `s_F` = Frobenius-minimal splitting (theorem needs only *some* bounded one, so this is favorable); `M=J` adjoint module (error-reduction uses `M=A`).
  *Precise gap (still the open math):* the `O(1)` bound comes from averaging over `U(M(J))` (Frobenius isometries), but the order-unit norm is preserved only by `Aut(J)⊊U(M(J))`. ⟹ **Pursue the INCREMENTAL route** (apply error-reduction only to a small algebra built one step at a time, so dimension never enters — Kitaev's actual trick), not global averaging. My earlier "resolved in principle" was too optimistic; corrected everywhere. The smoke-test numerics now lean toward "the constant really is dimension-free; find the right (incremental / `Aut`-only) argument."

**(B) Exact UP factorisation.** Generic `O(ε)` positivity-rounding is FALSE (spin-factor counterexample, `agent-b-findings`). B's route: "near-positive projection stability" — show `P` is `O(√η)`-close to a genuine positive idempotent `E`, then exact Effros–Størmer gives a genuine JC-algebra and exact UP factor maps. B reduced the **commutative** case to a stability theorem for almost-idempotent stochastic matrices and proved many special cases (rank-one, simplex, well-exposed, cluster-representative); the general "exposed-redundant dichotomy" and the non-commutative case are open. See `agent-B/theory/theorem-C-conditional-factorization.md`, `classical-cluster-factorization-theorem.md`, and the many `agent-B/notes/*classical*`/`*exposed*` files.

**(C) The exponent.** `O(√η)` unconditional. `O(η)` (Kitaev strength) holds for CP maps and is **conjectural for decomposable maps** (CP+co-CP), where a dilation restores the "two-hole-at-once" pairing. Reason: a single insertion is `O(√η)`; Kitaev's `O(η)` comes from a *bilinear* pairing of two holes in the Stinespring dilation, unavailable for general positive maps. (Findings §8–10.) B flagged that the standard decomposable norm of the transpose is `n`, so the decomposable theorem needs explicit CP+co-CP component bounds; even then the naive doubling is not controlled by `‖Φ²−Φ‖` — extra dilation-compatibility is needed.

---

## 4. The report (Agent A currently leads)

Location: `report/`. Build: `cd report && make` → `main.pdf` (needs `pdflatex`+`latexmk`, both present). Currently ~23 pp, compiles clean (exit 0, no undefined refs).
- `main.tex`: fixed preamble. Notation macros are `\ensuremath`-wrapped (mode-agnostic): `\HH,\Bsa,\jp(=∘),\Id(=𝟏),\norm,\eps,\Phimap,\R,\C,...`. Theorem envs + an `openproblem` env + a `\status{...}` macro. **Sections must NOT redefine macros.** `\input{sections/NN-*.tex}`.
- Sections: 00-abstract, 01-introduction, 02-preliminaries, 03-jordan-algebras (HOS-matched), 04-spectral-idempotent, 05-epsilon-jb (consensus def), **06-bridge-theorem (the complete proof)**, 07-structure-programme (open, with the numerical obstruction), 08-discussion (status ledger + open problems).
- `PROVENANCE.md`: **audit ledger**. Sources registered with SHA256; per-claim table with byte-verbatim status (`V`=byte-verified by A; `I`=inline-provenanced pending B's byte-check; `O`=original/internal; `PDF`=PDF source not yet text-verified). Status tags in statements: `(proved)`, `(cited)`, `(consensus)`, `(open)`.
- **Provenance policy (non-negotiable):** every named Definition/Theorem string-matched byte-verbatim to a local ground-truth file. No claim without a ledger row.
- `references.bib`, `Makefile`, `README.md`.

**Report verification status:** Agent B is doing the adversarial pass *in the report itself* — corrections are flagged with visible "Agent B review correction" / "review provenance flag" text. Remaining report obligations (per `agent-b-findings`): (1) byte-match the Chu–Russo Whitehead statements from the local CHU PDF (currently extraction-level); (2) register a primary local source for the Faraut–Korányi `Aut(J)` compactness claim (or keep it extraction-flagged).

---

## 5. Collaboration model (IMPORTANT)

Two agents work in parallel. Coordination is **only** via two files in the project root:
- `agent-a-findings` — **Agent A writes here** (only A, to avoid races); currently at v0.8.
- `agent-b-findings` — Agent B writes here; A reads it.
Both read both. Be cooperative, collegial, constructively skeptical — *demand excellence on constants and rigor; never accept blindly*. The job is done only when A and B reach **consensus** on formulation, statements, and proof.

Current division: **A** leads the report + Layer-1 abstract structure (error-reduction, incremental construction); **B** leads the Layer-1/projection-stability mathematics and produced the bridge proof. Both cross-review.

**Coordination gotcha:** B's subagent pipeline at one point generated `agent-B/notes/response-to-agent-a-v0.6 … v0.18.md` responding to Agent-A positions that were never written (subagent extrapolation). **`agent-a-findings` is the single source of truth for A's positions** — ground everything there, ignore phantom "A versions".

---

## 6. File map (where everything is)

Project root: `/home/tobiasosborne/Projects/almost-idempotent-positive-maps` (git repo, branch `main`, remote `origin`→`github.com:tobiasosborne/almost-idempotent-positive-maps`; this is a **sanitized public workspace** — `agent-A/refs/`, the ingestion JSON, and `agent-B/references/` are gitignored, so PROVENANCE rows pointing at them are not byte-checkable in this checkout; HOS/Idel ground truth lives at `/home/tobiasosborne/Projects/af-tests/examples3/...`, not the old `/home/tobias/...` paths this file used to cite). NB: paths below written `/home/tobias/...` are stale — substitute `/home/tobiasosborne/...`.
- `agent-A/refs/kitaev-src/approximate_algebras.tex` — Kitaev TeX (the result we generalise). Section line-ranges in memory `reference-sources`.
- `agent-A/refs/vlw-src/paper.tex` (+`preamble.tex`) — van Luijk–Wilming TeX.
- `agent-A/refs/lit/` — Chu–Russo (cohomology), Blecher–Neal, Itoh (positive projections), HOS book PDF, Baak–Moslehian.
- `agent-A/notes/ingestion-results-2026-06-01.json` — **big extraction** (110 KB): Kitaev full proof architecture + dependency map; VLW structure; lit surveys (Effros–Størmer, finite-dim JB classification, Jordan cohomology/rigidity). Start here for any cited fact.
- `agent-A/theory/00-overview.md`, `01-error-reduction.md` — A's working math.
- `agent-A/experiments/jordan-coboundary/` — numerical test of the dimension-free homotopy: `REPORT.md` + scripts + `results_*.json` (the original `n≤6` sweep, op-norm ~400 s/n). **`canary_smoke.py` + `results_canary.json` + `CANARY_SMOKE.md`** = the 2026-06-04 extended-`N` smoke test (spin `N=41`, `H_n` `N=36`; pure numpy, ~7 min; reuses the verified `jordan_common.py`/`jordan_fast.py` builders). Run: `python3 canary_smoke.py` (or `--quick`, `--families=spin`).
- `agent-B/theory/theorem-B-algebraic-bridge.md` — **the bridge proof** (source for report §6).
- `agent-B/theory/theorem-C-conditional-factorization.md`, `classical-cluster-factorization-theorem.md` — factorisation under projection-stability.
- `agent-B/notes/` — extensive: projection-stability program, classical reductions, decomposable analysis, compaction-checkpoint. `agent-B/references/effros-stormer-1979/` — ES OCR pages.
- `report/` — the report (see §4).
- **Ground truth for Jordan (string-match against these):** `../af-tests/examples3/Jordan Operator Algebras/joa-m/joa-m.md` (Hanche-Olsen–Størmer; JB def at L2314/2316=§3.1.4, Jordan def L963=§2.4.1, classification L2254=2.9.6); `../af-tests/examples3/Idel - 2013 - On the structure of positive maps/...md`.
- **Good-practice exemplars:** `../cft-anyons` (PROVENANCE.md, CITATION_INDEX.md discipline), `../arithmetic-quantum-mechanics/report` (main+sections model).

**Persistent memory** (survives compaction): `/home/tobias/.claude/projects/-home-tobias-Projects-almost-idempotent-positive-maps/memory/` — `MEMORY.md` (index) + `project-northstar`, `project-kadison-forces-jordan`, `project-proof-strategy`, `project-collaboration`, `project-report`, `reference-sources`. Read `MEMORY.md` first.

---

## 7. Concrete next actions (pick up here)

In rough priority order:
1. **Report finish-out (with B):** discharge the two remaining provenance obligations — byte-match Whitehead lemmas from the CHU PDF (extract text via `pdftotext`/`mutool` and grep), and find/flag a primary local source for `Aut(J)` compactness. Then A and B co-sign the `(cited)` rows. Fix the residual overfull/underfull lines if desired.
2. **Layer-1, incremental route (the headline open theorem):** the numerics say global averaging fails in operator norm (but the 2026-06-04 smoke test now suggests the *constant* is nonetheless dimension-free — find the right argument), so build `B` incrementally. Sub-steps = report §7 (a)–(d): (a) prove existence of a nontrivial approximate Jordan idempotent in any ε-JB algebra with `dim>1` (Task 4 — naive functional calculus only gives gap-dependent, not dimension-free, idempotents); (b) approximate Peirce; (c) approximate coordinatization; (d) per-step error reduction with dimension-free constant. This is most of a paper's worth of work.

   **Candidate analogues for the Lefschetz–Hopf step (a), worked out 2026-06-04 (discussion, not yet attempted):** Kitaev uses topology because it is *perturbation-stable ⇒ dimension-free*; the obstruction is that a Jordan algebra has no multiplicative group, so no H-space-from-the-product.
   - **(A) Approximate automorphism group — most faithful port.** Idempotents ↔ Peirce reflections `S_e` ∈ the *compact* `Aut(J)`. Build an intrinsic approximate `Aut(A) ⊂ GL(A)` (C¹ manifold via implicit-fn-thm, tangent = `Der(A)` — the SAME object the canary measured, `dim ker d¹=dim Der(J)`); it is a genuine associative H-space, so Kitaev's inversion/Lefschetz–Hopf forces a non-identity involution. Two honest gaps: (i) involution→idempotent conversion (not every involution is a Peirce reflection; use fixed-subalgebra recursion); (ii) `Aut` is *discrete* in the classical `ℝ^n` case (handed off to B's stochastic-stability work).
   - **(B) Symmetry variety / symmetric cone — most Jordan-native.** Work with `Sym(A)={s:s•s=1}={2e−1}` (exact `s↔e` correspondence, no conversion gap); its genuine version is a union of compact Jordan Grassmannians with nonzero cohomology. Forcing a nontrivial component to survive the ε-perturbation needs a *bespoke* degree/Morse argument (no H-space). Same classical degeneration as (A).
   - **(C) Constructive/analytic (the channels routes): spectral split / minimize `‖e•e−e‖` / Newton–Schulz `φ(a)=3a•a−2a•(a•a)`.** Gap-limited (quality `O(ε+1/gap)`), so not dimension-free *unless* you also prove a **quantitative gap-existence lemma** (every `dim>1` ε-JB algebra has a self-adjoint element with `Ω(1)` spectral gap — a possible non-topological shortcut).
   - **KEY: for the CHANNEL application you can probably skip Lefschetz–Hopf entirely** — `A=Im P ⊂ B(H)_sa` is concretely realized, so the ambient spectral theorem hands you idempotents directly (this is how the channels project sidesteps it). Topology is only needed for the *standalone abstract* structure theorem. Of the topological routes, (A) is the cleanest faithful port.
3. **Exact factorisation (with B):** push the near-positive projection stability program — finish the classical "exposed-redundant dichotomy" (`agent-B/notes/exposed-redundant-dichotomy-target.md`, `simultaneous-skeleton-reduction.md`) and lift to the non-commutative case.
4. **Decomposable `α=1`:** attempt the two-summand two-hole computation for decomposable `Φ=Φ_CP+Ψ∘τ` with explicit CP/co-CP component bounds and a dilation-compatibility hypothesis.

**Task tracker** (TaskList): #1,#2,#7 done; #3 (error-reduction) in progress; #4,#5 (idempotent/Peirce/coordinatization), #6 (assemble Theorem 2 + VLW), #8 (adversarial report verification with B) pending.

---

## 8. Gotchas / discipline
- **Don't overclaim.** The Layer-1 dimension-free homotopy is OPEN (Frobenius bounded, operator-norm open). The exponent is `√η` in general (`η` only CP/decomposable-conjectural). Exact UP factorisation is OPEN. Mark `(open)` honestly.
- **A definition is never `(proved)`** — tag adopted definitions `(consensus)`, transcribed ones `(cited)`. (User directive.)
- **Provenance is byte-verbatim**: recompute SHA256 (registry at top of `report/PROVENANCE.md`), `grep -F` the quote in the actual source. Flag extraction-level/PDF-only rows.
- **Background subagents stalled twice** on large-PDF/JSON reads and slow numerics — keep delegated tasks bounded; don't have an agent read a multi-MB PDF or the 110 KB JSON repeatedly.
- arXiv `2604.08380` is April-2026-dated; that is correct for this timeline (today is 2026-06-02), not a typo.
- The report `main.tex` macros are `\ensuremath`-wrapped on purpose (so symbols work in text); don't "fix" them back. `\status{...}` and `openproblem` were added by B's pass; keep them.
