# Verification of the W2d conditional assembly (opus, Claude family — 2-family rule)
VERDICT: ASSEMBLY: SOUND. (gpt-5.5-authored assembly, opus-verified 2026-06-10.)
Key checks: well-exposedness predicate is W-INDEPENDENT (separator-gap definition from the row set
alone) ⇒ no self-reference: W fixed before H; HLC's exposed-vertex disjunct is definitionally
impossible (every well-exposed vertex is in W ⊆ conv W at height 0), which is correct contrapositive
packaging — the surviving disjunct δ ≥ aH² is the open content. Case split exhaustive; constants
C' = max(4A, 1/√a) verified; reduction independent of all known-gapped lemmas (L5' maximizer-only,
L6 identity-frame-only, R1-vacuity, A2-stall) — those live inside HLC. Caveat: HLC itself OPEN.
