# w18_quadlit — quadratic-matrix-constraint literature recon: 10 ranked UNVERIFIED leads (codex research, 2026-06-11)
# Brief: /tmp/codex-sigma-wall/w18_quadlit/brief.md. ALL external claims are unverified leads until acquired + byte-pinned in refs/.

No network used. Every external item below is an **UNVERIFIED-LEAD** until acquired under `refs/` and byte-pinned. I read the requested context, especially [kernel-conjecture.tex](/home/tobias/Projects/almost-idempotent-positive-maps/agent-A/explorations/classical-portfolio/report/kernel-conjecture.tex:1) and [wave5-sigma-wall-parallel.md](/home/tobias/Projects/almost-idempotent-positive-maps/agent-A/explorations/classical-portfolio/notes/wave5-sigma-wall-parallel.md:700).

**Verdict:** best bets are not generic projection perturbation. The live leads are: semialgebraic error bounds for fixed `n`, `L1/L∞` contractive-projection structure as a W-free language, and Markov/nonnegative-matrix tools that might expose the missing anti-splitting principle.  
Calibrated `P(at least one lead is decisive)`: `0.32` for a dimension-free closure; `0.70` if fixed-`n` certification counts.

| rank | lead | exact statement to verify | source, confidence | campaign use | transfer risk |
|---:|---|---|---|---|---|
| 1 | Semialgebraic / Hoffman-Lojasiewicz error bounds | For compact semialgebraic `K` and polynomial constraints `F=0`, locally `dist(x,S) <= C ||F(x)||^theta`; under metric subregularity/linear regularity, `theta=1`. | Luo-Pang, “Error bounds for analytic systems…”, Math. Programming 1994, Thm 2.1; Robinson 1981 metric regularity; Hoffman 1952. Conf `0.75`. | Gives `delta >= c(n) H` or a certified exponent for each fixed `n`; identifies singular strata. | Constants/exponents may be useless in `n`; visible-set jumps and rank changes may force `theta<1`. |
| 2 | Douglas-Ando norm-one projections on `L1/L∞` | Norm-one projections on `L1` / finite `l∞` have averaging/conditional-expectation normal forms. Finite stochastic idempotents are the positive unital cases. | Douglas, “Contractive projections on an L1 space”, Pacific J. Math. 1965, main theorem; Ando, “Contractive projections in Lp spaces”, Pacific J. Math. 1966, Thm 2. Conf `0.85`. | W-free B-S normal-form language; possible route to projection-constant lower bound from hidden height. | Raw `O(delta)` distance to a norm-one projection is impossible by Hume; must target the audited B-S/height metric. |
| 3 | Nonnegative idempotent matrices | A nonnegative idempotent is permutation-similar to zero/transient columns plus rank-one irreducible diagonal blocks; stochastic case gives equal-input recurrent blocks and transient convex mixtures. | Flor, “On groups of non-negative matrices”, Compositio Math. 1969; Berman-Plemmons, *Nonnegative Matrices*, Ch. 3; H-M already local. Conf `0.80`. | Supplies the exact `delta=0` mechanism and quotient-component normal form. | Main steps are sign-rigid: `0=sum nonnegative terms` breaks under signed cancellation. |
| 4 | Markov chain tree theorem | For irreducible stochastic `K`, stationary weight `pi_i = tau_i / sum_j tau_j`, where `tau_i` sums products over directed spanning trees rooted at `i`. | Freidlin-Wentzell, *Random Perturbations*, 1984, Ch. 6; Leighton-Rivest / Anantharam-Tsoucas Markov chain tree theorem. Conf `0.75`. | Could convert aggregate shallow mass into a quotient path/tree product, i.e. anti-splitting. | Tree products decay exponentially in component size; may exactly reproduce the thin-chain obstruction. |
| 5 | Dobrushin/Birkhoff contraction | If a positive kernel has Hilbert diameter `Delta`, projective contraction factor is `tanh(Delta/4)`; if an idempotent stochastic kernel has ergodicity coefficient `<1`, rows collapse. | Birkhoff, “Extensions of Jentzsch’s theorem”, 1957; Bushell, Hilbert metric paper, 1973; Seneta, *Non-negative Matrices and Markov Chains*, Ch. 3. Conf `0.90`. | Rigorous source for the component finisher already rediscovered. | Does not handle `Delta=infty`; still needs path-product floor. |
| 6 | Meyer stochastic complementation / nearly reducible chains | For block stochastic `P`, stochastic complement `S=A+B(I-D)^{-1}C` preserves stationary behavior; perturbation bounds use group inverse/fundamental matrix. | Meyer, “Stochastic complementation…”, SIAM Rev. 1989; Schweitzer 1968 perturbation; Simon-Ando 1961 aggregation. Conf `0.90`. | Could formalize quotient/cluster aggregation and near-closed blocks. | Constants depend on spectral gaps/minimal masses; prior rank-complement route already hit this wall. |
| 7 | Kato perturbation of idempotents | If projections `P,Q` satisfy `||P-Q||<1`, then `W=QP+(1-Q)(1-P)` is invertible and `Q=WPW^{-1}`; Riesz projections vary analytically under spectral separation. | Kato, *Perturbation Theory for Linear Operators*, 2nd ed., Ch. I §4.6 and Ch. II. Conf `0.95`. | Handles clustered quotient robustness and similarity-smearing examples. | Similarity does not preserve positivity, row negativity, or exposedness geometry. |
| 8 | Riccati / invariant-subspace graph equations | In a block split, invariant graph `X` satisfies a quadratic Riccati equation such as `C + D X - X A - X B X = 0`; perturbation bounds use `sep(A,D)`. | Stewart-Sun, *Matrix Perturbation Theory*, 1990, Ch. V; Lancaster-Rodman, *Algebraic Riccati Equations*, 1995, Ch. 2. Conf `0.85`. | Local normal form for exact `P^2=P`; may isolate the shallow-fan degrees of freedom. | Gauge-dependent; positivity/LP hiddenness can disappear under coordinates. |
| 9 | Operator-algebra projection stability | If self-adjoint `a` has `||a^2-a||<1/4`, spectral projection `p=1_[1/2,∞)(a)` satisfies `||a-p|| <= (1-sqrt(1-4ε))/2`; norm-one positive/CP projections have conditional-expectation/Choi-Effros structure. | Blackadar K-theory text, projection lemma; Tomiyama 1957/58; Choi-Effros 1977; Christensen perturbation papers. Conf `0.80`. | Useful for `op-npps` analogy and noncommutative side. | Our `P` is exactly idempotent; defect is positivity, not idempotence. CP results likely over-assume. |
| 10 | SOS / Positivstellensatz / copositive programming | If compact semialgebraic `K` has Archimedean quadratic module and polynomial `f>0` on `K`, then `f` has an SOS certificate; Lasserre hierarchy converges. | Putinar, Indiana Univ. Math. J. 1993, main theorem; Lasserre, SIAM J. Optim. 2001; Parrilo 2003. Conf `0.90`. | Full hiddenness-aware fixed-`n` certificates; stronger than failed scalar-chain SOS. | Degree may explode; strict positivity needed; not dimension-free. |

**Top-3 Attack Sketches**

1. **Semialgebraic route:** define a W-free semialgebraic distance to the H-M/B-S normal-form locus for fixed `n`. Apply error-bound theory to `P^2=P`, `P1=1`, row-negativity `<=delta`, and the normal-form strata. If exponent `1` holds on every stratum, fixed-`n` linear law follows; failures identify exact singular mechanisms.

2. **Douglas-Ando route:** treat `P` as a unital projection on `l∞^n` with `||P||∞→∞ = 1+2delta`. Norm-one structure gives the correct B-S normal forms. The useful target is not raw distance to a stochastic idempotent, but a lower bound: hidden height forces projection norm excess `>= c H`.

3. **Markov-tree / quotient route:** work on the multiplicity quotient `Q`. Use tree formulas or Dobrushin coefficients to turn aggregate shallow mass into a path/tree product lower bound. If this fails, the failure should be an explicit shallow-fan construction, exactly the current anti-splitting frontier.

**Shopping List**

1. Douglas 1965; Ando 1966; plus searches for “almost contractive projections”, “λ-projections”, “projection constants of `l∞^n`”.
2. Luo-Pang 1994; Robinson metric regularity; Ioffe/Kurdyka error-bound surveys.
3. Flor 1969 and Berman-Plemmons nonnegative-idempotent chapters.
4. Meyer 1989, Seneta, Schweitzer, Simon-Ando, Markov chain tree theorem sources.
5. Kato; Stewart-Sun; Lancaster-Rodman.
6. Putinar/Lasserre/Parrilo for full hiddenness-aware SOS certificates.
7. Low priority: sign-pattern idempotent matrix papers, for obstruction templates rather than proof closure.