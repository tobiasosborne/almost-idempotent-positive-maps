# w25_hm112: Hognas-Mukherjea Theorem 1.12 — the SIGNED structure theorem nobody read

You are a codex (gpt-5.5) analyst+prover. USER DISCOVERY (2026-06-11): H-M
Theorem 1.12 (the theorem directly after 1.11) is a structure theorem for
arbitrary REAL (signed) idempotent matrices — exactly the campaign's objects —
with a CONVERSE. The entire campaign (autopsies w14/w15_hmloci targeted
Thm 1.11/1.16/2.2) never read it. Autopsy it and connect it to the frontier.
Repo root: /home/tobias/Projects/almost-idempotent-positive-maps.
Workdir (writable): /tmp/codex-sigma-wall/w25_hm112.
PROGRESS PROTOCOL: short line per stage to progress.md.
ARTIFACT RULE: long form to proof.md, NEVER answer.md.

## SOURCE
refs/hognas-mukherjea-2011/hognas-mukherjea-2011.txt. Theorem 1.12 statement +
proof: lines ~2245-2400 (grep "Theorem 1.12"); the referenced Exercise 1.43
(grep for it); Sect. 1.7 (the infinite-dimensional nonnegative proof of 1.11);
Theorem 1.16 (:2767-2777, the stochastic specialization) for comparison.
Quote loci (line ranges) for every step you use; extraction-level provenance.

## CAMPAIGN CONTEXT (read after the source)
1. agent-A/explorations/classical-portfolio/OVERVIEW.md — the bird's-eye map
   (just written; §3 frontier: the local-law assembly is broken at visibility
   hypotheses; the GLOBAL gap = is every small-delta idempotent near the H-M
   family).
2. notes/swarm-answers/w14_autopsy.md — the delta=0 proof's SIGN-RIGID steps
   (zero-sum closure, zero-pattern symmetry, positive diagonal). KEY QUESTION:
   Thm 1.12's identities (1.1)-(1.4) look like EXACT SIGNED replacements for
   exactly those steps — assess.
3. notes/swarm-answers/w18_semigroup.md — the fixed-space form P = sum u_s l_s^T
   (rows live in a k-dim space): Thm 1.12 refines this with the
   proportional-class partition + sum rules. What does 1.12 add beyond it?
4. notes/swarm-answers/w19_leftcone.md — the refuted proximity lemma (its n=4
   counterexample family: where do its rows sit in 1.12's partition?).

## TASKS
1. RESTATE Theorem 1.12 in campaign language (rows as points; delta = row
   negative mass; our P additionally has P1 = 1 — specialize: what do
   (1.2)-(1.4) become with row sums 1? T_r empty as in the stochastic case?).
   Verify the converse direction's exact hypotheses. Byte-quote the statement
   (grep -F verified, with line ranges) for refs externals.
2. AUTOPSY the proof (it is IN THE TEXT right after the statement, lines
   ~2290-2400): step by step; note it needs NO positivity at all (it is exact
   linear algebra) — confirm, and extract the constructive content: given P,
   how are the classes C_s found (proportional-row classes of k independent
   rows), how canonical/non-unique is the partition (the text warns
   "nonunique" — quantify what choices are free).
3. THE BRIDGE QUESTION (the payoff — think hard): for OUR P (P^2 = P, P1 = 1,
   row negative mass <= delta, delta small), what do 1.12's data + the sum
   rules FORCE quantitatively?
   - Each C_s has proportional rows with rank-one restriction: with entries
     >= -delta and row sums 1, how close must a C_s class be to an equal-input
     (H-M recurrent) block? (This is the w12 collapse conclusion obtained
     ALGEBRAICALLY, without Birkhoff contraction — derive the modulus.)
   - The B rows: mixtures with coefficients a_t(i) satisfying (1.4) — with
     near-positivity, how close to CONVEX mixtures (a_t(i) >= -something,
     summing to ~1)? Compare to the transient rows of the H-M form.
   - The identities (1.2)/(1.3) (sum = 1 within class, = 0 across classes):
     these are the EXACT signed sum rules whose delta=0 versions were the
     sign-rigid zero-sum closures. Do they, with the delta-budget, force the
     cross-class terms to be SMALL (the missing anti-splitting/pinning
     ingredient?) or can cancellation hide large terms (exhibit which)?
   - SYNTHESIS: does 1.12 reduce the GLOBAL gap ("every small-delta idempotent
     is near the H-M family") to bounded, named estimates? Write the candidate
     lemma chain with explicit first lemmas in display math. Where exactly
     could it fail (the nonuniqueness? B-class coefficients unbounded? the
     proportionality factors gamma(h, u_s) signed and large?). Check the
     w19_leftcone counterexample family and the certified w16/w17 instances
     against your chain (numerics allowed: python3/numpy; the instances are in
     experiments/out/w16_cert_audit/ and w17_cert_audit/ as rational JSON).
4. Relation to the variety programme: 1.12 is a GLOBAL exact parametrization —
   does it subsume/complement the local chart (the broken assembly's
   visibility issue might be bypassed if 1.12 gives the nearest H-M point
   DIRECTLY: nearest-point candidate = normalize each C_s class to equal-input
   + clip B coefficients to the simplex — test this constructively on the
   certified instances: compute the resulting distance vs delta!).

## DELIVERABLE (verdict-first; long form to proof.md)
VERDICT: BRIDGE VIABLE (the lemma chain to the global gap, display math,
with the constructive nearest-point test results) / PARTIALLY (what 1.12
gives + the named obstruction) / NOT USEFUL (why precisely). Then the
restatement + loci table (grep-verified quotes), the proof autopsy, the
quantitative derivations, numerics, and calibrated P(1.12 route reaches the
global statement), P(your reading survives audit).
